from turtle import*

def friso(n, lado):
    for i in range(n):
        forward(lado)
        left(90)
        forward(lado)
        right(90)
        forward(lado)
        right(90)
        forward(lado)
        left(90)
